 "name": "mini-celula-automatizada",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "three": "^0.179.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}. Vite.config.js: import { defineConfig } from "vite";

export default defineConfig({
    server: {
        open: true
    }
});
